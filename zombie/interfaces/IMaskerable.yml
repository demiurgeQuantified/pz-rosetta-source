version: '1.1'
languages:
  java:
    packages:
      zombie.interfaces:
        IMaskerable:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          methods:
          - name: getMask
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: Mask
                full: zombie.core.textures.Mask
              notes: the collision mask
            notes: returns the collision mask
