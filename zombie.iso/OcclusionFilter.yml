version: '1.1'
languages:
  java:
    packages:
      zombie.iso:
        OcclusionFilter:
          modifiers:
          - public
          - static
          staticFields:
            All:
              name: All
              modifiers:
              - public
              - static
              - final
              type:
                basic: OcclusionFilter
            Left:
              name: Left
              modifiers:
              - public
              - static
              - final
              type:
                basic: OcclusionFilter
            Right:
              name: Right
              modifiers:
              - public
              - static
              - final
              type:
                basic: OcclusionFilter
          staticMethods:
          - name: values
            modifiers:
            - public
            - static
            return:
              type:
                basic: OcclusionFilter
              notes: an array containing the constants of this enum class, in the
                order they are declared
            notes: Returns an array containing the constants of this enum class, in
              the order they are declared.
          javaType: enum
          extends: Enum
