version: '1.1'
languages:
  java:
    packages:
      java.awt:
        RenderingHints.Key:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Defines the base type of all keys used along with the\n RenderingHints\
            \ class to control various\n algorithm choices in the rendering and imaging\
            \ pipelines.\n Instances of this class are immutable and unique which\n\
            \ means that tests for matches can be made using the\n == operator instead\
            \ of the more expensive\n equals() method."
          fields:
            privatekey:
              name: privatekey
              modifiers:
              - private
              type:
                basic: int
                full: int
          staticFields:
            identitymap:
              name: identitymap
              modifiers:
              - private
              - static
              type:
                basic: HashMap
                full: java.util.HashMap<java.lang.Object, java.lang.Object>
          constructors:
          - modifiers:
            - protected
            parameters:
            - name: privatekey
              type:
                basic: int
                full: int
              notes: the specified key
            notes: "Construct a key using the indicated private key.  Each\n subclass\
              \ of Key maintains its own unique domain of integer\n keys.  No two\
              \ objects with the same integer key and of the\n same specific subclass\
              \ can be constructed.  An exception\n will be thrown if an attempt is\
              \ made to construct another\n object of a given class with the same\
              \ integer key as a\n pre-existing instance of that subclass of Key."
          methods:
          - name: equals
            modifiers:
            - public
            - final
            parameters:
            - name: o
              type:
                basic: Object
                full: java.lang.Object
              notes: the reference object with which to compare.
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if this object is the same as the obj\n          argument;\
                \ false otherwise."
            notes: "The equals method for all Key objects will return the same\n result\
              \ as the equality operator '=='."
          - name: getIdentity
            modifiers:
            - private
            return:
              type:
                basic: String
                full: java.lang.String
          - name: hashCode
            modifiers:
            - public
            - final
            return:
              type:
                basic: int
                full: int
              notes: a hash code value for this object.
            notes: "The hash code for all Key objects will be the same as the\n system\
              \ identity code of the object as defined by the\n System.identityHashCode()\
              \ method."
          - name: intKey
            modifiers:
            - protected
            - final
            return:
              type:
                basic: int
                full: int
              notes: "the private integer key that the subclass\n instantiated this\
                \ Key with."
            notes: "Returns the private integer key that the subclass\n instantiated\
              \ this Key with."
          - name: isCompatibleValue
            modifiers:
            - public
            - abstract
            parameters:
            - name: val
              type:
                basic: Object
                full: java.lang.Object
              notes: the Object to test for validity
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if val is valid;\n         false otherwise."
            notes: "Returns true if the specified object is a valid value\n for this\
              \ Key."
          staticMethods:
          - name: recordIdentity
            modifiers:
            - private
            - static
            parameters:
            - name: arg0
              type:
                basic: RenderingHints.Key
                full: java.awt.RenderingHints$Key
            return:
              type:
                basic: void
                full: void
