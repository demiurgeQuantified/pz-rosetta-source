version: '1.1'
languages:
  java:
    packages:
      java.awt.desktop:
        OpenFilesEvent:
          extends: java.awt.desktop.FilesEvent
          modifiers:
          - public
          - final
          notes: Event sent when the app is asked to open a list of files.
          fields:
            searchTerm:
              name: searchTerm
              modifiers:
              - package
              type:
                basic: String
                full: java.lang.String
          staticFields:
            serialVersionUID:
              name: serialVersionUID
              modifiers:
              - private
              - static
              type:
                basic: long
                full: long
          constructors:
          - modifiers:
            - public
            parameters:
            - name: files
              type:
                basic: List
                full: java.util.List<java.io.File>
              notes: the list of files
            - name: searchTerm
              type:
                basic: String
                full: java.lang.String
              notes: the search term
            notes: Constructs an OpenFilesEvent.
          methods:
          - name: getSearchTerm
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the search term used to find the files
            notes: "Gets the search term. The platform may optionally provide the\
              \ search term\n that was used to find the files. This is for example\
              \ the case on MacOS,\n when the files were opened using the Spotlight\
              \ search menu or a Finder\n search window.\n \n This is useful for highlighting\
              \ the search term in the documents when\n they are opened."
