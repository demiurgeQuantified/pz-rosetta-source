version: '1.1'
languages:
  java:
    packages:
      java.awt:
        GraphicsDevice.WindowTranslucency:
          extends: java.lang.Enum<java.awt.GraphicsDevice$WindowTranslucency>
          modifiers:
          - public
          - final
          notes: Kinds of translucency supported by the underlying system.
          staticFields:
            $VALUES:
              name: $VALUES
              modifiers:
              - private
              - static
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency[]
            PERPIXEL_TRANSLUCENT:
              name: PERPIXEL_TRANSLUCENT
              modifiers:
              - public
              - static
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency
              notes: "Represents support in the underlying system for windows that\n\
                \ contain or might contain pixels with arbitrary alpha values\n between\
                \ and including 0.0 and 1.0."
            PERPIXEL_TRANSPARENT:
              name: PERPIXEL_TRANSPARENT
              modifiers:
              - public
              - static
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency
              notes: "Represents support in the underlying system for windows each\
                \ pixel\n of which is guaranteed to be either completely opaque, with\n\
                \ an alpha value of 1.0, or completely transparent, with an alpha\n\
                \ value of 0.0."
            TRANSLUCENT:
              name: TRANSLUCENT
              modifiers:
              - public
              - static
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency
              notes: "Represents support in the underlying system for windows all\
                \ of\n the pixels of which have the same alpha value between or including\n\
                \ 0.0 and 1.0."
          constructors:
          - modifiers:
            - private
          staticMethods:
          - name: $values
            modifiers:
            - private
            - static
            return:
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency[]
          - name: valueOf
            modifiers:
            - public
            - static
            parameters:
            - name: arg0
              type:
                basic: String
                full: java.lang.String
            return:
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency
          - name: values
            modifiers:
            - public
            - static
            return:
              type:
                basic: GraphicsDevice.WindowTranslucency
                full: java.awt.GraphicsDevice$WindowTranslucency[]
              notes: an array containing the constants of this enum class, in the
                order they are declared
            notes: 'Returns an array containing the constants of this enum class,
              in

              the order they are declared.'
