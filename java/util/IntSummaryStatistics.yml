version: '1.1'
languages:
  java:
    packages:
      java.util:
        IntSummaryStatistics:
          extends: java.lang.Object
          modifiers:
          - public
          fields:
            count:
              name: count
              modifiers:
              - private
              type:
                basic: long
                full: long
            max:
              name: max
              modifiers:
              - private
              type:
                basic: int
                full: int
            min:
              name: min
              modifiers:
              - private
              type:
                basic: int
                full: int
            sum:
              name: sum
              modifiers:
              - private
              type:
                basic: long
                full: long
          constructors:
          - modifiers:
            - public
            notes: "Constructs an empty instance with zero count, zero sum,\n Integer.MAX_VALUE\
              \ min, Integer.MIN_VALUE max and zero\n average."
          - modifiers:
            - public
            parameters:
            - name: count
              type:
                basic: long
                full: long
              notes: the count of values
            - name: min
              type:
                basic: int
                full: int
              notes: the minimum value
            - name: max
              type:
                basic: int
                full: int
              notes: the maximum value
            - name: sum
              type:
                basic: long
                full: long
              notes: the sum of all values
            notes: "Constructs a non-empty instance with the specified count,\n min,\
              \ max, and sum.\n\n If count is zero then the remaining arguments are\
              \ ignored and\n an empty instance is constructed.\n\n If the arguments\
              \ are inconsistent then an IllegalArgumentException\n is thrown.  The\
              \ necessary consistent argument conditions are:\n \n   count >= 0\n\
              \   min <= max"
          methods:
          - name: accept
            modifiers:
            - public
            parameters:
            - name: value
              type:
                basic: int
                full: int
              notes: the input value
            return:
              type:
                basic: void
                full: void
            notes: Records a new value into the summary information
          - name: combine
            modifiers:
            - public
            parameters:
            - name: other
              type:
                basic: IntSummaryStatistics
                full: java.util.IntSummaryStatistics
              notes: another IntSummaryStatistics
            return:
              type:
                basic: void
                full: void
            notes: Combines the state of another IntSummaryStatistics into this one.
          - name: getAverage
            modifiers:
            - public
            - final
            return:
              type:
                basic: double
                full: double
              notes: the arithmetic mean of values, or zero if none
            notes: "Returns the arithmetic mean of values recorded, or zero if no\
              \ values have been\n recorded."
          - name: getCount
            modifiers:
            - public
            - final
            return:
              type:
                basic: long
                full: long
              notes: the count of values
            notes: Returns the count of values recorded.
          - name: getMax
            modifiers:
            - public
            - final
            return:
              type:
                basic: int
                full: int
              notes: the maximum value, or Integer.MIN_VALUE if none
            notes: "Returns the maximum value recorded, or Integer.MIN_VALUE if no\n\
              \ values have been recorded."
          - name: getMin
            modifiers:
            - public
            - final
            return:
              type:
                basic: int
                full: int
              notes: the minimum value, or Integer.MAX_VALUE if none
            notes: "Returns the minimum value recorded, or Integer.MAX_VALUE if no\n\
              \ values have been recorded."
          - name: getSum
            modifiers:
            - public
            - final
            return:
              type:
                basic: long
                full: long
              notes: the sum of values, or zero if none
            notes: "Returns the sum of values recorded, or zero if no values have\
              \ been\n recorded."
          - name: toString
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
