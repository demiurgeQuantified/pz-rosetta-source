version: '1.1'
languages:
  java:
    packages:
      java.net:
        ContentHandler:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          constructors:
          - modifiers:
            - public
            notes: Constructor for subclasses to call.
          methods:
          - name: getContent
            modifiers:
            - public
            - abstract
            parameters:
            - name: urlc
              type:
                basic: URLConnection
                full: java.net.URLConnection
              notes: a URL connection.
            return:
              type:
                basic: Object
                full: java.lang.Object
              notes: the object read by the ContentHandler.
            notes: "Given a URL connect stream positioned at the beginning of the\n\
              \ representation of an object, this method reads that stream and\n creates\
              \ an object from it."
          - name: getContent
            modifiers:
            - public
            parameters:
            - name: urlc
              type:
                basic: URLConnection
                full: java.net.URLConnection
              notes: a URL connection.
            - name: classes
              type:
                basic: Class
                full: java.lang.Class[]
              notes: an array of types requested
            return:
              type:
                basic: Object
                full: java.lang.Object
              notes: "the object read by the ContentHandler that is\n            \
                \     the first match of the suggested types or\n                \
                \ null if none of the requested  are supported."
            notes: "Given a URL connect stream positioned at the beginning of the\n\
              \ representation of an object, this method reads that stream and\n creates\
              \ an object that matches one of the types specified.\n\n The default\
              \ implementation of this method should call\n getContent(URLConnection)\n\
              \ and screen the return type for a match of the suggested types."
