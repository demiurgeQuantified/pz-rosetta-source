version: '1.1'
languages:
  java:
    packages:
      java.time.format:
        DateTimeParseException:
          extends: java.time.DateTimeException
          modifiers:
          - public
          notes: "An exception thrown when an error occurs during parsing.\n \n This\
            \ exception includes the text being parsed and the error index."
          fields:
            errorIndex:
              name: errorIndex
              modifiers:
              - private
              type:
                basic: int
                full: int
            parsedString:
              name: parsedString
              modifiers:
              - private
              type:
                basic: String
                full: java.lang.String
          staticFields:
            serialVersionUID:
              name: serialVersionUID
              modifiers:
              - private
              - static
              type:
                basic: long
                full: long
          constructors:
          - modifiers:
            - public
            parameters:
            - name: message
              type:
                basic: String
                full: java.lang.String
              notes: the message to use for this exception, may be null
            - name: parsedData
              type:
                basic: CharSequence
                full: java.lang.CharSequence
              notes: the parsed text, should not be null
            - name: errorIndex
              type:
                basic: int
                full: int
              notes: the index in the parsed string that was invalid, should be a
                valid index
            notes: Constructs a new exception with the specified message.
          - modifiers:
            - public
            parameters:
            - name: message
              type:
                basic: String
                full: java.lang.String
              notes: the message to use for this exception, may be null
            - name: parsedData
              type:
                basic: CharSequence
                full: java.lang.CharSequence
              notes: the parsed text, should not be null
            - name: errorIndex
              type:
                basic: int
                full: int
              notes: the index in the parsed string that was invalid, should be a
                valid index
            - name: cause
              type:
                basic: Throwable
                full: java.lang.Throwable
              notes: the cause exception, may be null
            notes: Constructs a new exception with the specified message and cause.
          methods:
          - name: getErrorIndex
            modifiers:
            - public
            return:
              type:
                basic: int
                full: int
              notes: the index in the parsed string that was invalid, should be a
                valid index
            notes: Returns the index where the error was found.
          - name: getParsedString
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the string that was being parsed, should not be null.
            notes: Returns the string that was being parsed.
