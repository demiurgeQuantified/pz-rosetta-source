version: '1.1'
languages:
  java:
    packages:
      java.util.stream:
        LongStream.LongMapMultiConsumer:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation that accepts a long-valued argument\n and\
            \ a LongConsumer, and returns no result. This functional interface is\n\
            \ used by LongStream.mapMulti\n to replace a long value with zero or more\
            \ long values.\n\n This is a functional interface\n whose functional method\
            \ is accept(long, LongConsumer)."
          methods:
          - name: accept
            modifiers:
            - public
            - abstract
            parameters:
            - name: value
              type:
                basic: long
                full: long
              notes: the long value coming from upstream
            - name: lc
              type:
                basic: LongConsumer
                full: java.util.function.LongConsumer
              notes: a LongConsumer accepting the mapped values
            return:
              type:
                basic: void
                full: void
            notes: "Replaces the given value with zero or more values by feeding the\
              \ mapped\n values to the lc consumer."
