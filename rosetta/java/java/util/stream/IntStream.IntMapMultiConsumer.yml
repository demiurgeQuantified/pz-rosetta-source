version: '1.1'
languages:
  java:
    packages:
      java.util.stream:
        IntStream.IntMapMultiConsumer:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation that accepts an int-valued argument\n and\
            \ an IntConsumer, and returns no result. This functional interface is\n\
            \ used by IntStream.mapMulti\n to replace an int value with zero or more\
            \ int values.\n\n This is a functional interface\n whose functional method\
            \ is accept(int, IntConsumer)."
          methods:
          - name: accept
            modifiers:
            - public
            - abstract
            parameters:
            - name: value
              type:
                basic: int
                full: int
              notes: the int value coming from upstream
            - name: ic
              type:
                basic: IntConsumer
                full: java.util.function.IntConsumer
              notes: an IntConsumer accepting the mapped values
            return:
              type:
                basic: void
                full: void
            notes: "Replaces the given value with zero or more values by feeding the\
              \ mapped\n values to the ic consumer."
