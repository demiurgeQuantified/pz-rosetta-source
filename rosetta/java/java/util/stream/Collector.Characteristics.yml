version: '1.1'
languages:
  java:
    packages:
      java.util.stream:
        Collector.Characteristics:
          extends: java.lang.Enum<java.util.stream.Collector$Characteristics>
          modifiers:
          - public
          - final
          notes: "Characteristics indicating properties of a Collector, which can\n\
            \ be used to optimize reduction implementations."
          staticFields:
            $VALUES:
              name: $VALUES
              modifiers:
              - private
              - static
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics[]
            CONCURRENT:
              name: CONCURRENT
              modifiers:
              - public
              - static
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics
              notes: "Indicates that this collector is concurrent, meaning that\n\
                \ the result container can support the accumulator function being\n\
                \ called concurrently with the same result container from multiple\n\
                \ threads.\n\n If a CONCURRENT collector is not also UNORDERED,\n\
                \ then it should only be evaluated concurrently if applied to an\n\
                \ unordered data source."
            IDENTITY_FINISH:
              name: IDENTITY_FINISH
              modifiers:
              - public
              - static
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics
              notes: "Indicates that the finisher function is the identity function\
                \ and\n can be elided.  If set, it must be the case that an unchecked\
                \ cast\n from A to R will succeed."
            UNORDERED:
              name: UNORDERED
              modifiers:
              - public
              - static
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics
              notes: "Indicates that the collection operation does not commit to preserving\n\
                \ the encounter order of input elements.  (This might be true if the\n\
                \ result container has no intrinsic order, such as a Set.)"
          constructors:
          - modifiers:
            - private
          staticMethods:
          - name: $values
            modifiers:
            - private
            - static
            return:
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics[]
          - name: valueOf
            modifiers:
            - public
            - static
            parameters:
            - name: arg0
              type:
                basic: String
                full: java.lang.String
            return:
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics
          - name: values
            modifiers:
            - public
            - static
            return:
              type:
                basic: Collector.Characteristics
                full: java.util.stream.Collector$Characteristics[]
              notes: an array containing the constants of this enum class, in the
                order they are declared
            notes: 'Returns an array containing the constants of this enum class,
              in

              the order they are declared.'
