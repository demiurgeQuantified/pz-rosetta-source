version: '1.1'
languages:
  java:
    packages:
      java.util.zip:
        Checksum:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: An interface representing a data checksum.
          methods:
          - name: getValue
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: long
                full: long
              notes: the current checksum value
            notes: Returns the current checksum value.
          - name: reset
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: void
                full: void
            notes: Resets the checksum to its initial value.
          - name: update
            modifiers:
            - public
            - abstract
            parameters:
            - name: b
              type:
                basic: int
                full: int
              notes: the byte to update the checksum with
            return:
              type:
                basic: void
                full: void
            notes: Updates the current checksum with the specified byte.
          - name: update
            modifiers:
            - public
            parameters:
            - name: b
              type:
                basic: byte
                full: byte[]
              notes: the array of bytes to update the checksum with
            return:
              type:
                basic: void
                full: void
            notes: Updates the current checksum with the specified array of bytes.
          - name: update
            modifiers:
            - public
            - abstract
            parameters:
            - name: b
              type:
                basic: byte
                full: byte[]
              notes: the byte array to update the checksum with
            - name: off
              type:
                basic: int
                full: int
              notes: the start offset of the data
            - name: len
              type:
                basic: int
                full: int
              notes: the number of bytes to use for the update
            return:
              type:
                basic: void
                full: void
            notes: Updates the current checksum with the specified array of bytes.
          - name: update
            modifiers:
            - public
            parameters:
            - name: buffer
              type:
                basic: ByteBuffer
                full: java.nio.ByteBuffer
              notes: the ByteBuffer to update the checksum with
            return:
              type:
                basic: void
                full: void
            notes: "Updates the current checksum with the bytes from the specified\
              \ buffer.\n\n The checksum is updated with the remaining bytes in the\
              \ buffer, starting\n at the buffer's position. Upon return, the buffer's\
              \ position will be\n updated to its limit; its limit will not have been\
              \ changed."
