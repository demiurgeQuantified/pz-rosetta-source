version: '1.1'
languages:
  java:
    packages:
      java.util.regex:
        MatchResult:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "The result of a match operation.\n\n This interface contains query\
            \ methods used to determine the\n results of a match against a regular\
            \ expression. The match boundaries,\n groups and group boundaries can\
            \ be seen but not modified through\n a MatchResult."
          methods:
          - name: end
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
              notes: The offset after the last character matched
            notes: Returns the offset after the last character matched.
          - name: end
            modifiers:
            - public
            - abstract
            parameters:
            - name: group
              type:
                basic: int
                full: int
              notes: The index of a capturing group in this matcher's pattern
            return:
              type:
                basic: int
                full: int
              notes: "The offset after the last character captured by the group,\n\
                \          or -1 if the match was successful\n          but the group\
                \ itself did not match anything"
            notes: "Returns the offset after the last character of the subsequence\n\
              \ captured by the given group during this match.\n\n  Capturing groups\
              \ are indexed from left\n to right, starting at one.  Group zero denotes\
              \ the entire pattern, so\n the expression m.end(0) is equivalent to\n\
              \ m.end()."
          - name: group
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "The (possibly empty) subsequence matched by the previous match,\n\
                \         in string form"
            notes: "Returns the input subsequence matched by the previous match.\n\
              \n  For a matcher m with input sequence s,\n the expressions m.group()\
              \ and\n s.substring(m.start(), m.end())\n are equivalent.  \n\n  Note\
              \ that some patterns, for example a*, match the empty\n string.  This\
              \ method will return the empty string when the pattern\n successfully\
              \ matches the empty string in the input."
          - name: group
            modifiers:
            - public
            - abstract
            parameters:
            - name: group
              type:
                basic: int
                full: int
              notes: The index of a capturing group in this matcher's pattern
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "The (possibly empty) subsequence captured by the group\n   \
                \       during the previous match, or null if the group\n        \
                \  failed to match part of the input"
            notes: "Returns the input subsequence captured by the given group during\
              \ the\n previous match operation.\n\n  For a matcher m, input sequence\
              \ s, and group index\n g, the expressions m.group(g) and\n s.substring(m.start(g\n\
              \ ), m.end(g))\n are equivalent.  \n\n  Capturing groups are indexed\
              \ from left\n to right, starting at one.  Group zero denotes the entire\
              \ pattern, so\n the expression m.group(0) is equivalent to m.group().\n\
              \ \n\n  If the match was successful but the group specified failed to\
              \ match\n any part of the input sequence, then null is returned. Note\n\
              \ that some groups, for example (a*), match the empty string.\n This\
              \ method will return the empty string when such a group successfully\n\
              \ matches the empty string in the input."
          - name: groupCount
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
              notes: The number of capturing groups in this matcher's pattern
            notes: "Returns the number of capturing groups in this match result's\
              \ pattern.\n\n  Group zero denotes the entire pattern by convention.\
              \ It is not\n included in this count.\n\n  Any non-negative integer\
              \ smaller than or equal to the value\n returned by this method is guaranteed\
              \ to be a valid group index for\n this matcher."
          - name: start
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
              notes: The index of the first character matched
            notes: Returns the start index of the match.
          - name: start
            modifiers:
            - public
            - abstract
            parameters:
            - name: group
              type:
                basic: int
                full: int
              notes: The index of a capturing group in this matcher's pattern
            return:
              type:
                basic: int
                full: int
              notes: "The index of the first character captured by the group,\n  \
                \        or -1 if the match was successful but the group\n       \
                \   itself did not match anything"
            notes: "Returns the start index of the subsequence captured by the given\
              \ group\n during this match.\n\n  Capturing groups are indexed from\
              \ left\n to right, starting at one.  Group zero denotes the entire pattern,\
              \ so\n the expression m.start(0) is equivalent to\n m.start()."
