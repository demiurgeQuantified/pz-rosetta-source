version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        DoubleUnaryOperator:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation on a single double-valued operand that produces\n\
            \ a double-valued result.  This is the primitive type specialization of\n\
            \ UnaryOperator for double.\n\n This is a functional interface\n whose\
            \ functional method is applyAsDouble(double)."
          methods:
          - name: andThen
            modifiers:
            - public
            parameters:
            - name: after
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
              notes: the operator to apply after this operator is applied
            return:
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
              notes: "a composed operator that first applies this operator and then\n\
                \ applies the after operator"
            notes: "Returns a composed operator that first applies this operator to\n\
              \ its input, and then applies the after operator to the result.\n If\
              \ evaluation of either operator throws an exception, it is relayed to\n\
              \ the caller of the composed operator."
          - name: applyAsDouble
            modifiers:
            - public
            - abstract
            parameters:
            - name: operand
              type:
                basic: double
                full: double
              notes: the operand
            return:
              type:
                basic: double
                full: double
              notes: the operator result
            notes: Applies this operator to the given operand.
          - name: compose
            modifiers:
            - public
            parameters:
            - name: before
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
              notes: the operator to apply before this operator is applied
            return:
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
              notes: "a composed operator that first applies the before\n operator\
                \ and then applies this operator"
            notes: "Returns a composed operator that first applies the before\n operator\
              \ to its input, and then applies this operator to the result.\n If evaluation\
              \ of either operator throws an exception, it is relayed to\n the caller\
              \ of the composed operator."
          - name: lambda$andThen$1
            modifiers:
            - private
            parameters:
            - name: arg0
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
            - name: arg1
              type:
                basic: double
                full: double
            return:
              type:
                basic: double
                full: double
          - name: lambda$compose$0
            modifiers:
            - private
            parameters:
            - name: arg0
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
            - name: arg1
              type:
                basic: double
                full: double
            return:
              type:
                basic: double
                full: double
          staticMethods:
          - name: identity
            modifiers:
            - public
            - static
            return:
              type:
                basic: DoubleUnaryOperator
                full: java.util.function.DoubleUnaryOperator
              notes: a unary operator that always returns its input argument
            notes: Returns a unary operator that always returns its input argument.
          - name: lambda$identity$2
            modifiers:
            - private
            - static
            parameters:
            - name: arg0
              type:
                basic: double
                full: double
            return:
              type:
                basic: double
                full: double
