version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        DoubleBinaryOperator:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation upon two double-valued operands and producing\
            \ a\n double-valued result.   This is the primitive type specialization\
            \ of\n BinaryOperator for double.\n\n This is a functional interface\n\
            \ whose functional method is applyAsDouble(double, double)."
          methods:
          - name: applyAsDouble
            modifiers:
            - public
            - abstract
            parameters:
            - name: left
              type:
                basic: double
                full: double
              notes: the first operand
            - name: right
              type:
                basic: double
                full: double
              notes: the second operand
            return:
              type:
                basic: double
                full: double
              notes: the operator result
            notes: Applies this operator to the given operands.
