version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        ToDoubleFunction:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents a function that produces a double-valued result.  This\
            \ is the\n double-producing primitive specialization for Function.\n\n\
            \ This is a functional interface\n whose functional method is applyAsDouble(Object)."
          methods:
          - name: applyAsDouble
            modifiers:
            - public
            - abstract
            parameters:
            - name: value
              type:
                basic: T
                full: T
              notes: the function argument
            return:
              type:
                basic: double
                full: double
              notes: the function result
            notes: Applies this function to the given argument.
