version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        IntBinaryOperator:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation upon two int-valued operands and producing\
            \ an\n int-valued result.   This is the primitive type specialization\
            \ of\n BinaryOperator for int.\n\n This is a functional interface\n whose\
            \ functional method is applyAsInt(int, int)."
          methods:
          - name: applyAsInt
            modifiers:
            - public
            - abstract
            parameters:
            - name: left
              type:
                basic: int
                full: int
              notes: the first operand
            - name: right
              type:
                basic: int
                full: int
              notes: the second operand
            return:
              type:
                basic: int
                full: int
              notes: the operator result
            notes: Applies this operator to the given operands.
