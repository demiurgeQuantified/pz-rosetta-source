version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        LongToDoubleFunction:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents a function that accepts a long-valued argument and produces\
            \ a\n double-valued result.  This is the long-to-double primitive\n specialization\
            \ for Function.\n\n This is a functional interface\n whose functional\
            \ method is applyAsDouble(long)."
          methods:
          - name: applyAsDouble
            modifiers:
            - public
            - abstract
            parameters:
            - name: value
              type:
                basic: long
                full: long
              notes: the function argument
            return:
              type:
                basic: double
                full: double
              notes: the function result
            notes: Applies this function to the given argument.
