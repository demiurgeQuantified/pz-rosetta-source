version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        LongBinaryOperator:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents an operation upon two long-valued operands and producing\
            \ a\n long-valued result.   This is the primitive type specialization\
            \ of\n BinaryOperator for long.\n\n This is a functional interface\n whose\
            \ functional method is applyAsLong(long, long)."
          methods:
          - name: applyAsLong
            modifiers:
            - public
            - abstract
            parameters:
            - name: left
              type:
                basic: long
                full: long
              notes: the first operand
            - name: right
              type:
                basic: long
                full: long
              notes: the second operand
            return:
              type:
                basic: long
                full: long
              notes: the operator result
            notes: Applies this operator to the given operands.
