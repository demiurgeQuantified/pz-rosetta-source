version: '1.1'
languages:
  java:
    packages:
      java.util.function:
        ToIntBiFunction:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "Represents a function that accepts two arguments and produces an\
            \ int-valued\n result.  This is the int-producing primitive specialization\
            \ for\n BiFunction.\n\n This is a functional interface\n whose functional\
            \ method is applyAsInt(Object, Object)."
          methods:
          - name: applyAsInt
            modifiers:
            - public
            - abstract
            parameters:
            - name: t
              type:
                basic: T
                full: T
              notes: the first function argument
            - name: u
              type:
                basic: U
                full: U
              notes: the second function argument
            return:
              type:
                basic: int
                full: int
              notes: the function result
            notes: Applies this function to the given arguments.
