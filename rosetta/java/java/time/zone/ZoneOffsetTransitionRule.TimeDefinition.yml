version: '1.1'
languages:
  java:
    packages:
      java.time.zone:
        ZoneOffsetTransitionRule.TimeDefinition:
          extends: java.lang.Enum<java.time.zone.ZoneOffsetTransitionRule$TimeDefinition>
          modifiers:
          - public
          - final
          notes: "A definition of the way a local time can be converted to the actual\n\
            \ transition date-time.\n \n Time zone rules are expressed in one of three\
            \ ways:\n \n Relative to UTC\n Relative to the standard offset in force\n\
            \ Relative to the wall offset (what you would see on a clock on the wall)"
          staticFields:
            $VALUES:
              name: $VALUES
              modifiers:
              - private
              - static
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition[]
            STANDARD:
              name: STANDARD
              modifiers:
              - public
              - static
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition
              notes: The local date-time is expressed in terms of the standard offset.
            UTC:
              name: UTC
              modifiers:
              - public
              - static
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition
              notes: The local date-time is expressed in terms of the UTC offset.
            WALL:
              name: WALL
              modifiers:
              - public
              - static
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition
              notes: The local date-time is expressed in terms of the wall offset.
          constructors:
          - modifiers:
            - private
          methods:
          - name: createDateTime
            modifiers:
            - public
            parameters:
            - name: dateTime
              type:
                basic: LocalDateTime
                full: java.time.LocalDateTime
              notes: the local date-time, not null
            - name: standardOffset
              type:
                basic: ZoneOffset
                full: java.time.ZoneOffset
              notes: the standard offset, not null
            - name: wallOffset
              type:
                basic: ZoneOffset
                full: java.time.ZoneOffset
              notes: the wall offset, not null
            return:
              type:
                basic: LocalDateTime
                full: java.time.LocalDateTime
              notes: the date-time relative to the wall/before offset, not null
            notes: "Converts the specified local date-time to the local date-time\
              \ actually\n seen on a wall clock.\n \n This method converts using the\
              \ type of this enum.\n The output is defined relative to the 'before'\
              \ offset of the transition.\n \n The UTC type uses the UTC offset.\n\
              \ The STANDARD type uses the standard offset.\n The WALL type returns\
              \ the input date-time.\n The result is intended for use with the wall-offset."
          staticMethods:
          - name: $values
            modifiers:
            - private
            - static
            return:
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition[]
          - name: valueOf
            modifiers:
            - public
            - static
            parameters:
            - name: arg0
              type:
                basic: String
                full: java.lang.String
            return:
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition
          - name: values
            modifiers:
            - public
            - static
            return:
              type:
                basic: ZoneOffsetTransitionRule.TimeDefinition
                full: java.time.zone.ZoneOffsetTransitionRule$TimeDefinition[]
              notes: an array containing the constants of this enum class, in the
                order they are declared
            notes: 'Returns an array containing the constants of this enum class,
              in

              the order they are declared.'
