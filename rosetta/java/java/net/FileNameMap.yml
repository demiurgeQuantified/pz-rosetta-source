version: '1.1'
languages:
  java:
    packages:
      java.net:
        FileNameMap:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "A simple interface which provides a mechanism to map\n between a\
            \ file name and a MIME type string."
          methods:
          - name: getContentTypeFor
            modifiers:
            - public
            - abstract
            parameters:
            - name: fileName
              type:
                basic: String
                full: java.lang.String
              notes: the specified file name
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "a String indicating the MIME\n type for the specified file name."
            notes: Gets the MIME type for the specified file name.
