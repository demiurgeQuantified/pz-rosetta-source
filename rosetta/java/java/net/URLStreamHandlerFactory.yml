version: '1.1'
languages:
  java:
    packages:
      java.net:
        URLStreamHandlerFactory:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "This interface defines a factory for URL stream\n protocol handlers.\n\
            \n  A URL stream handler factory is used as specified in the\n URL constructor."
          methods:
          - name: createURLStreamHandler
            modifiers:
            - public
            - abstract
            parameters:
            - name: protocol
              type:
                basic: String
                full: java.lang.String
              notes: "the protocol (\"ftp\",\n                     \"http\", \"nntp\"\
                , etc.)."
            return:
              type:
                basic: URLStreamHandler
                full: java.net.URLStreamHandler
              notes: "a URLStreamHandler for the specific protocol, or \n        \
                \  null if this factory cannot create a handler for the specific\n\
                \          protocol"
            notes: "Creates a new URLStreamHandler instance with the specified\n protocol."
