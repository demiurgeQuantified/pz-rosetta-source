version: '1.1'
languages:
  java:
    packages:
      java.awt.datatransfer:
        FlavorMap:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "A two-way Map between \"natives\" (Strings), which correspond to\n\
            \ platform-specific data formats, and \"flavors\" (DataFlavors), which\
            \ correspond\n to platform-independent MIME types. FlavorMaps need not\
            \ be symmetric, but\n typically are."
          methods:
          - name: getFlavorsForNatives
            modifiers:
            - public
            - abstract
            parameters:
            - name: natives
              type:
                basic: String
                full: java.lang.String[]
              notes: "an array of Strings which will be the key set of\n         the\
                \ returned Map. If null is specified, a mapping\n         of all String\
                \ natives currently known to this\n         FlavorMap to their corresponding\
                \ DataFlavors will\n         be returned."
            return:
              type:
                basic: Map
                full: java.util.Map<java.lang.String, java.awt.datatransfer.DataFlavor>
              notes: "a java.util.Map of String natives to\n         DataFlavors"
            notes: "Returns a Map of the specified String natives to their\n corresponding\
              \ DataFlavor. The returned Map is a\n modifiable copy of this FlavorMap's\
              \ internal data. Client code is\n free to modify the Map without affecting\
              \ this object."
          - name: getNativesForFlavors
            modifiers:
            - public
            - abstract
            parameters:
            - name: flavors
              type:
                basic: DataFlavor
                full: java.awt.datatransfer.DataFlavor[]
              notes: "an array of DataFlavors which will be the key set\n        \
                \ of the returned Map. If null is specified, a\n         mapping of\
                \ all DataFlavors currently known to this\n         FlavorMap to their\
                \ corresponding String natives\n         will be returned."
            return:
              type:
                basic: Map
                full: java.util.Map<java.awt.datatransfer.DataFlavor, java.lang.String>
              notes: "a java.util.Map of DataFlavors to String\n         natives"
            notes: "Returns a Map of the specified DataFlavors to their\n corresponding\
              \ String native. The returned Map is a\n modifiable copy of this FlavorMap's\
              \ internal data. Client code is\n free to modify the Map without affecting\
              \ this object."
