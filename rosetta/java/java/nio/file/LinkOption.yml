version: '1.1'
languages:
  java:
    packages:
      java.nio.file:
        LinkOption:
          extends: java.lang.Enum<java.nio.file.LinkOption>
          modifiers:
          - public
          - final
          notes: Defines the options as to how symbolic links are handled.
          staticFields:
            $VALUES:
              name: $VALUES
              modifiers:
              - private
              - static
              type:
                basic: LinkOption
                full: java.nio.file.LinkOption[]
            NOFOLLOW_LINKS:
              name: NOFOLLOW_LINKS
              modifiers:
              - public
              - static
              type:
                basic: LinkOption
                full: java.nio.file.LinkOption
              notes: Do not follow symbolic links.
          constructors:
          - modifiers:
            - private
          staticMethods:
          - name: $values
            modifiers:
            - private
            - static
            return:
              type:
                basic: LinkOption
                full: java.nio.file.LinkOption[]
          - name: valueOf
            modifiers:
            - public
            - static
            parameters:
            - name: arg0
              type:
                basic: String
                full: java.lang.String
            return:
              type:
                basic: LinkOption
                full: java.nio.file.LinkOption
          - name: values
            modifiers:
            - public
            - static
            return:
              type:
                basic: LinkOption
                full: java.nio.file.LinkOption[]
              notes: an array containing the constants of this enum class, in the
                order they are declared
            notes: 'Returns an array containing the constants of this enum class,
              in

              the order they are declared.'
