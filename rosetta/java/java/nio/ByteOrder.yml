version: '1.1'
languages:
  java:
    packages:
      java.nio:
        ByteOrder:
          extends: java.lang.Object
          modifiers:
          - public
          - final
          notes: A typesafe enumeration for byte orders.
          fields:
            name:
              name: name
              modifiers:
              - private
              type:
                basic: String
                full: java.lang.String
          staticFields:
            BIG_ENDIAN:
              name: BIG_ENDIAN
              modifiers:
              - public
              - static
              type:
                basic: ByteOrder
                full: java.nio.ByteOrder
              notes: "Constant denoting big-endian byte order.  In this order, the\
                \ bytes of a\n multibyte value are ordered from most significant to\
                \ least significant."
            LITTLE_ENDIAN:
              name: LITTLE_ENDIAN
              modifiers:
              - public
              - static
              type:
                basic: ByteOrder
                full: java.nio.ByteOrder
              notes: "Constant denoting little-endian byte order.  In this order,\
                \ the bytes of\n a multibyte value are ordered from least significant\
                \ to most\n significant."
            NATIVE_ORDER:
              name: NATIVE_ORDER
              modifiers:
              - private
              - static
              type:
                basic: ByteOrder
                full: java.nio.ByteOrder
          constructors:
          - modifiers:
            - private
            parameters:
            - name: arg0
              type:
                basic: String
                full: java.lang.String
          methods:
          - name: toString
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
          staticMethods:
          - name: nativeOrder
            modifiers:
            - public
            - static
            return:
              type:
                basic: ByteOrder
                full: java.nio.ByteOrder
              notes: "The native byte order of the hardware upon which this Java\n\
                \          virtual machine is running"
            notes: "Retrieves the native byte order of the underlying platform.\n\n\
              \  This method is defined so that performance-sensitive Java code can\n\
              \ allocate direct buffers with the same byte order as the hardware.\n\
              \ Native code libraries are often more efficient when such buffers are\n\
              \ used."
