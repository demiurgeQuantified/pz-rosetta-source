version: '1.1'
languages:
  java:
    packages:
      java.lang.invoke:
        TypeDescriptor:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: An entity that has a type descriptor.
          methods:
          - name: descriptorString
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the descriptor string for this TypeDescriptor object
            notes: "Returns the descriptor string for this TypeDescriptor object.\n\
              \n If this TypeDescriptor object can be described in nominal form,\n\
              \ then this method returns a type descriptor as specified in JVMS 4.3.\n\
              \ The result descriptor string can be used to produce\n a nominal descriptor.\n\
              \n Otherwise, the result string is not a type descriptor.\n No nominal\
              \ descriptor\n can be produced from the result string."
