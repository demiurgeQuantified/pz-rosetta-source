version: '1.1'
languages:
  java:
    packages:
      java.lang:
        StackWalker.StackFrame:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "A StackFrame object represents a method invocation returned by\n\
            \ StackWalker.\n\n  The getDeclaringClass() method may be unsupported\
            \ as determined\n by the stack walking options of a stack walker."
          methods:
          - name: getByteCodeIndex
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
              notes: "the index to the code array of the Code attribute\n        \
                \ containing the execution point represented by this stack frame,\n\
                \         or a negative number if the method is native."
            notes: "Returns the index to the code array of the Code attribute\n containing\
              \ the execution point represented by this stack frame.\n The code array\
              \ gives the actual bytes of Java Virtual Machine code\n that implement\
              \ the method."
          - name: getClassName
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "the binary name of the declaring class of the method\n     \
                \    represented by this stack frame"
            notes: "Gets the binary name\n of the declaring class of the method represented\
              \ by this stack frame."
          - name: getDeclaringClass
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: Class
                full: java.lang.Class<?>
              notes: "the declaring Class of the method represented by\n this stack\
                \ frame"
            notes: "Gets the declaring Class for the method represented by\n this\
              \ stack frame."
          - name: getDescriptor
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "the descriptor of the method represented by\n         this stack\
                \ frame"
            notes: "Returns the descriptor of the method represented by\n this stack\
              \ frame as defined by\n The Java Virtual Machine Specification."
          - name: getFileName
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "the name of the file containing the execution point\n      \
                \   represented by this stack frame, or null if\n         this information\
                \ is unavailable."
            notes: "Returns the name of the source file containing the execution point\n\
              \ represented by this stack frame.  Generally, this corresponds\n to\
              \ the SourceFile attribute of the relevant class\n file as defined by\
              \ The Java Virtual Machine Specification.\n In some systems, the name\
              \ may refer to some source code unit\n other than a file, such as an\
              \ entry in a source repository."
          - name: getLineNumber
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
              notes: "the line number of the source line containing the execution\n\
                \         point represented by this stack frame, or a negative number\
                \ if\n         this information is unavailable."
            notes: "Returns the line number of the source line containing the execution\n\
              \ point represented by this stack frame.  Generally, this is\n derived\
              \ from the LineNumberTable attribute of the relevant\n class file as\
              \ defined by The Java Virtual Machine\n Specification."
          - name: getMethodName
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the name of the method represented by this stack frame
            notes: Gets the name of the method represented by this stack frame.
          - name: getMethodType
            modifiers:
            - public
            return:
              type:
                basic: MethodType
                full: java.lang.invoke.MethodType
              notes: the MethodType for this stack frame
            notes: "Returns the MethodType representing the parameter types and\n\
              \ the return type for the method represented by this stack frame."
          - name: isNativeMethod
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if the method containing the execution point\n        \
                \ represented by this stack frame is a native method."
            notes: "Returns true if the method containing the execution point\n represented\
              \ by this stack frame is a native method."
          - name: toStackTraceElement
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: StackTraceElement
                full: java.lang.StackTraceElement
              notes: StackTraceElement for this stack frame.
            notes: Gets a StackTraceElement for this stack frame.
