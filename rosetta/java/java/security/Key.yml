version: '1.1'
languages:
  java:
    packages:
      java.security:
        Key:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          staticFields:
            serialVersionUID:
              name: serialVersionUID
              modifiers:
              - public
              - static
              type:
                basic: long
                full: long
              notes: "The class fingerprint that is set to indicate\n serialization\
                \ compatibility with a previous\n version of the class."
          methods:
          - name: getAlgorithm
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the name of the algorithm associated with this key.
            notes: "Returns the standard algorithm name for this key. For\n example,\
              \ \"DSA\" would indicate that this key is a DSA key.\n See the key related\
              \ sections (KeyFactory, KeyGenerator,\n KeyPairGenerator, and SecretKeyFactory)\
              \ in the \n Java Security Standard Algorithm Names Specification\n for\
              \ information about standard key algorithm names."
          - name: getEncoded
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: byte
                full: byte[]
              notes: "the encoded key, or null if the key does not support\n encoding."
            notes: "Returns the key in its primary encoding format, or null\n if this\
              \ key does not support encoding."
          - name: getFormat
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: String
                full: java.lang.String
              notes: the primary encoding format of the key.
            notes: "Returns the name of the primary encoding format of this key,\n\
              \ or null if this key does not support encoding.\n The primary encoding\
              \ format is\n named in terms of the appropriate ASN.1 data format, if\
              \ an\n ASN.1 specification for this key exists.\n For example, the name\
              \ of the ASN.1 data format for public\n keys is SubjectPublicKeyInfo,\
              \ as\n defined by the X.509 standard; in this case, the returned format\
              \ is\n \"X.509\". Similarly,\n the name of the ASN.1 data format for\
              \ private keys is\n PrivateKeyInfo,\n as defined by the PKCS #8 standard;\
              \ in this case, the returned format is\n \"PKCS#8\"."
