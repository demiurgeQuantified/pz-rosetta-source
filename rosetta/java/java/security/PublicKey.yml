version: '1.1'
languages:
  java:
    packages:
      java.security:
        PublicKey:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "A public key. This interface contains no methods or constants.\n\
            \ It merely serves to group (and provide type safety for) all public key\n\
            \ interfaces.\n\n Note: The specialized public key interfaces extend this\
            \ interface.\n See, for example, the DSAPublicKey interface in\n java.security.interfaces."
          staticFields:
            serialVersionUID:
              name: serialVersionUID
              modifiers:
              - public
              - static
              type:
                basic: long
                full: long
              notes: "The class fingerprint that is set to indicate serialization\n\
                \ compatibility with a previous version of the class."
