version: '1.1'
languages:
  java:
    packages:
      javax.accessibility:
        AccessibleRelationSet:
          extends: java.lang.Object
          modifiers:
          - public
          notes: "Class AccessibleRelationSet determines a component's relation set.\n\
            \ The relation set of a component is a set of AccessibleRelation\n objects\
            \ that describe the component's relationships with other components."
          fields:
            relations:
              name: relations
              modifiers:
              - protected
              type:
                basic: Vector
                full: java.util.Vector<javax.accessibility.AccessibleRelation>
              notes: "Each entry in the Vector represents an\n AccessibleRelation."
          constructors:
          - modifiers:
            - public
            notes: Creates a new empty relation set.
          - modifiers:
            - public
            parameters:
            - name: relations
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation[]
              notes: "an array of AccessibleRelation describing the\n         relation\
                \ set"
            notes: "Creates a new relation with the initial set of relations contained\
              \ in the\n array of relations passed in. Duplicate entries are ignored."
          methods:
          - name: add
            modifiers:
            - public
            parameters:
            - name: relation
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation
              notes: the relation to add to the relation set
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if relation is added to the relation set;\n         false\
                \ if the relation set is unchanged"
            notes: "Adds a new relation to the current relation set. If the relation\
              \ is\n already in the relation set, the target(s) of the specified relation\
              \ is\n merged with the target(s) of the existing relation. Otherwise,\
              \ the new\n relation is added to the relation set."
          - name: addAll
            modifiers:
            - public
            parameters:
            - name: relations
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation[]
              notes: "AccessibleRelation array describing the\n         relation set"
            return:
              type:
                basic: void
                full: void
            notes: "Adds all of the relations to the existing relation set. Duplicate\
              \ entries\n are ignored."
          - name: clear
            modifiers:
            - public
            return:
              type:
                basic: void
                full: void
            notes: Removes all the relations from the current relation set.
          - name: contains
            modifiers:
            - public
            parameters:
            - name: key
              type:
                basic: String
                full: java.lang.String
              notes: the AccessibleRelation key
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if the relation is in the relation set; otherwise\n   \
                \      false"
            notes: "Returns whether the relation set contains a relation that matches\
              \ the\n specified key."
          - name: get
            modifiers:
            - public
            parameters:
            - name: key
              type:
                basic: String
                full: java.lang.String
              notes: the AccessibleRelation key
            return:
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation
              notes: "the relation, if one exists, that matches the specified key.\n\
                \         Otherwise, null is returned."
            notes: Returns the relation that matches the specified key.
          - name: remove
            modifiers:
            - public
            parameters:
            - name: relation
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation
              notes: the relation to remove from the relation set
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if the relation is in the relation set;\n         false\
                \ if the relation set is unchanged"
            notes: "Removes a relation from the current relation set. If the relation\
              \ is not\n in the set, the relation set will be unchanged and the return\
              \ value will\n be false. If the relation is in the relation set, it\
              \ will be\n removed from the set and the return value will be true."
          - name: size
            modifiers:
            - public
            return:
              type:
                basic: int
                full: int
              notes: the number of relations in the relation set
            notes: Returns the number of relations in the relation set.
          - name: toArray
            modifiers:
            - public
            return:
              type:
                basic: AccessibleRelation
                full: javax.accessibility.AccessibleRelation[]
              notes: AccessibleRelation array contacting the current relation
            notes: "Returns the current relation set as an array of\n AccessibleRelation."
          - name: toString
            modifiers:
            - public
            return:
              type:
                basic: String
                full: java.lang.String
