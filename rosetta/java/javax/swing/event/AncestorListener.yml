version: '1.1'
languages:
  java:
    packages:
      javax.swing.event:
        AncestorListener:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "AncestorListener\n\n Interface to support notification when changes\
            \ occur to a JComponent or one\n of its ancestors.  These include movement\
            \ and when the component becomes\n visible or invisible, either by the\
            \ setVisible() method or by being added\n or removed from the component\
            \ hierarchy."
          methods:
          - name: ancestorAdded
            modifiers:
            - public
            - abstract
            parameters:
            - name: event
              type:
                basic: AncestorEvent
                full: javax.swing.event.AncestorEvent
              notes: "an AncestorEvent signifying a change in an\n              ancestor-component's\
                \ display-status"
            return:
              type:
                basic: void
                full: void
            notes: "Called when the source or one of its ancestors is made visible\n\
              \ either by setVisible(true) being called or by its being\n added to\
              \ the component hierarchy.  The method is only called\n if the source\
              \ has actually become visible.  For this to be true\n all its parents\
              \ must be visible and it must be in a hierarchy\n rooted at a Window"
          - name: ancestorMoved
            modifiers:
            - public
            - abstract
            parameters:
            - name: event
              type:
                basic: AncestorEvent
                full: javax.swing.event.AncestorEvent
              notes: "an AncestorEvent signifying a change in an\n              ancestor-component's\
                \ display-status"
            return:
              type:
                basic: void
                full: void
            notes: Called when either the source or one of its ancestors is moved.
          - name: ancestorRemoved
            modifiers:
            - public
            - abstract
            parameters:
            - name: event
              type:
                basic: AncestorEvent
                full: javax.swing.event.AncestorEvent
              notes: "an AncestorEvent signifying a change in an\n              ancestor-component's\
                \ display-status"
            return:
              type:
                basic: void
                full: void
            notes: "Called when the source or one of its ancestors is made invisible\n\
              \ either by setVisible(false) being called or by its being\n removed\
              \ from the component hierarchy.  The method is only called\n if the\
              \ source has actually become invisible.  For this to be true\n at least\
              \ one of its parents must by invisible or it is not in\n a hierarchy\
              \ rooted at a Window"
