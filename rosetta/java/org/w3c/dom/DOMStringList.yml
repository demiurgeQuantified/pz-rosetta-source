version: '1.1'
languages:
  java:
    packages:
      org.w3c.dom:
        DOMStringList:
          extends: java.lang.Object
          modifiers:
          - public
          - abstract
          notes: "The DOMStringList interface provides the abstraction of an\n ordered\
            \ collection of DOMString values, without defining or\n constraining how\
            \ this collection is implemented. The items in the\n DOMStringList are\
            \ accessible via an integral index, starting\n from 0.\n See also the\
            \ Document Object Model (DOM) Level 3 Core Specification."
          methods:
          - name: contains
            modifiers:
            - public
            - abstract
            parameters:
            - name: str
              type:
                basic: String
                full: java.lang.String
              notes: The string to look for.
            return:
              type:
                basic: boolean
                full: boolean
              notes: "true if the string has been found,\n   false otherwise."
            notes: Test if a string is part of this DOMStringList.
          - name: getLength
            modifiers:
            - public
            - abstract
            return:
              type:
                basic: int
                full: int
            notes: "The number of DOMStrings in the list. The range of valid\n child\
              \ node indices is 0 to length-1 inclusive."
          - name: item
            modifiers:
            - public
            - abstract
            parameters:
            - name: index
              type:
                basic: int
                full: int
              notes: Index into the collection.
            return:
              type:
                basic: String
                full: java.lang.String
              notes: "The DOMString at the indexth\n   position in the DOMStringList,\
                \ or null if\n   that is not a valid index."
            notes: "Returns the indexth item in the collection. If\n index is greater\
              \ than or equal to the number of\n DOMStrings in the list, this returns\
              \ null."
